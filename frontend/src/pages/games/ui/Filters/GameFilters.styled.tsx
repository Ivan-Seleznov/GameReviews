import styled from "styled-components";

export const GameFiltersContainer = styled.div({
  display: "flex",
  flexDirection: "column",
  gap: "10px",
});
export const CheckboxFiltersContainer = styled.div({
  display: "flex",
  flexDirection: "column",
  gap: "5px",
});
